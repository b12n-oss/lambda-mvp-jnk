# lambda-mvp-jnk guide

- [The Runtime API loop](runtime-api-loop.md) -- the contract, and
  `http.jank`'s design.
- [Container image build](container-image-build.md) -- why a
  container image instead of a `provided.al2023` zip, the APT-package
  build, the ECR push.
- [Cold vs. warm boot](cold-warm-boot.md) -- what `bb bench` measures
  and why the numbers aren't comparable to the Jolt sibling's.

See also the [project README](https://github.com/b12n-oss/lambda-mvp-jnk/blob/main/README.md).
